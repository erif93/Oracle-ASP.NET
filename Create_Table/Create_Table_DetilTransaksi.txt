
CREATE TABLE  "DETIL_TRANSAKSI" 
   (	"ID" NUMBER NOT NULL ENABLE, 
	"ID_ITEM" NUMBER NOT NULL ENABLE, 
	"ID_TRANSAKSI" NUMBER NOT NULL ENABLE, 
	"QTY" NUMBER, 
	"TOTAL" NUMBER, 
	"CREATEDATE" DATE, 
	 CONSTRAINT "DETIL_TRANSAKSI_PK" PRIMARY KEY ("ID") ENABLE
   ) ;ALTER TABLE  "DETIL_TRANSAKSI" ADD CONSTRAINT "DETIL_TRANSAKSI_FK" FOREIGN KEY ("ID_ITEM")
	  REFERENCES  "ITEM" ("ID") ENABLE;ALTER TABLE  "DETIL_TRANSAKSI" ADD CONSTRAINT "DETIL_TRANSAKSI_FK2" FOREIGN KEY ("ID_TRANSAKSI")
	  REFERENCES  "TRANSMASTER" ("ID") ENABLE;

CREATE OR REPLACE TRIGGER  "BI_DETIL_TRANSAKSI" 
  before insert on "DETIL_TRANSAKSI"               
  for each row  
begin   
  if :NEW."ID" is null then 
    select "DETIL_TRANSAKSI_SEQ".nextval into :NEW."ID" from dual; 
  end if; 
end; 

/
ALTER TRIGGER  "BI_DETIL_TRANSAKSI" ENABLE;